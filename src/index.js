"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
let lista = [];
document.addEventListener('DOMContentLoaded', () => {
    var _a, _b, _c, _d, _e;
    let statue = document.getElementById('statue');
    let artwork = document.getElementById('artwork');
    let nev = document.getElementById('nev');
    let ertek = document.getElementById('ertek');
    let ev = document.getElementById('ev');
    let magassag = document.getElementById('magassag');
    (_a = document.getElementById('nev')) === null || _a === void 0 ? void 0 : _a.addEventListener('change', () => {
        if (artwork.checked) {
        }
    });
    (_b = document.getElementById('ertek')) === null || _b === void 0 ? void 0 : _b.addEventListener('change', () => {
        if (artwork.checked) {
        }
    });
    (_c = document.getElementById('ev')) === null || _c === void 0 ? void 0 : _c.addEventListener('change', () => {
        if (artwork.checked) {
        }
    });
    (_d = document.getElementById('magassag')) === null || _d === void 0 ? void 0 : _d.addEventListener('change', () => {
        if (artwork.checked) {
        }
    });
    (_e = document.getElementById('felvetel')) === null || _e === void 0 ? void 0 : _e.addEventListener('click', () => {
        if (artwork.checked) {
        }
    });
});
